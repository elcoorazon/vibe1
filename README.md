# vibe1

Next.js + Tailwind calculator app with keyboard support and in-memory history API.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`
- `npm run test`

## API

- `GET /api/history`
- `POST /api/history` with `{ "expression": "1+2", "result": "3" }`
- `DELETE /api/history`
.
